import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcedenio.geolocationSQLite',
  appName: 'geolocationSQLite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
